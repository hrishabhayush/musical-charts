import type { Hex, PublicClient } from 'viem';
export type CheckFaucetParams = {
    address: Hex;
    publicClient: PublicClient;
    faucetUrl: string;
    minBalanceWei?: bigint | number;
    minBalanceEther?: bigint | number;
};
export type CheckFaucetResult = {
    sent: false;
} | {
    sent: true;
    hash: Hex;
    txUrl?: string;
};
export declare const checkFaucet: ({ address, publicClient, faucetUrl, minBalanceWei, minBalanceEther, }: CheckFaucetParams) => Promise<CheckFaucetResult>;
//# sourceMappingURL=faucet.d.ts.map